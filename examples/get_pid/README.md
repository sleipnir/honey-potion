# Get Pid Example

This example demonstrates how to print the current process PID and another number using Honey Potion and eBPF. It shows the use of C-like arguments in print statements.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```


