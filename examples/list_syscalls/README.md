# List Syscalls Example

This example demonstrates how to list and identify different syscalls using Honey Potion for eBPF. It prints messages for specific syscall IDs and can be extended to handle more types.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```


