# Recursive Factorial Example

This example implements a recursive factorial function in Elixir and uses it within an eBPF program. It demonstrates recursion and printing results with Honey Potion.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```


