# Case

This example demonstrates the use of the `case` statement in Elixir within an eBPF program using Honey Potion. It matches on different values and prints the result using `bpf_printk`.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```
