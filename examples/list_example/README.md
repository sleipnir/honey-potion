# List Example

This example demonstrates tuple and list pattern matching in Elixir using Honey Potion for eBPF. It extracts values from a tuple inside a list and prints the result.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```


