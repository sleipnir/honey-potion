# Linked List Example

This example demonstrates how to build and pattern match on a linked list in Elixir using Honey Potion for eBPF. It prints the value of an element extracted from the list.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```


