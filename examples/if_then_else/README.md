# If Then Else Example

This example demonstrates the use of `if` and `else` statements in Elixir within an eBPF program using Honey Potion. It prints different messages depending on the condition.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```


