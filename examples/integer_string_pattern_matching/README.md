# Pattern Matching Example

This example demonstrates integer and string pattern matching in Elixir using Honey Potion for eBPF. It shows how to match values and print a success message.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```


