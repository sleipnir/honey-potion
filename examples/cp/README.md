# Constant Propagation Example

This example demonstrates constant propagation in Elixir using Honey Potion for eBPF. It computes a sum of several constants and returns the result.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```


