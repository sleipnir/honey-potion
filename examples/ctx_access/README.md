# Ctx Access Example

This example shows how to access context (`ctx`) arguments in an eBPF program using Honey Potion. It prints the PID of the callee and the target process.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```


