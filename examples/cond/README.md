# Cond Example

This example demonstrates the use of the `cond` statement in Elixir within an eBPF program using Honey Potion. It evaluates several conditions and prints a message depending on the value of a variable.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```


