# Tuple Example

This example demonstrates tuple pattern matching in Elixir using Honey Potion for eBPF programs. The program matches a nested tuple and prints the value of a variable extracted from the tuple.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```
