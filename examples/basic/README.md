# Basic Example

This is a minimal example for testing basic compilation with Honey Potion and eBPF. It simply returns the value 1.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```


