# Dead Code Elimination Exemple

This example demonstrates dead code elimination (DCE) in Elixir using Honey Potion. It shows how unreachable code is removed from the final eBPF program.

## How to compile

Run the following commands inside this directory:

```sh
mix format
mix deps.get
mix compile
```


